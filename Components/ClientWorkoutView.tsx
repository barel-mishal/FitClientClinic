import { ScrollView, StyleSheet, Text } from "react-native"

const ClientWorkoutView = (workouts: any) => {
    return <ScrollView>
        <Text>hello workouts</Text>
    </ScrollView>
}

const styles = StyleSheet.create({

});

export default ClientWorkoutView;